import styles from './Principal.module.css';

export default function Card({id, nome, descricao, telefone}) {
    return (
        <div className={styles.container_principal}>
            <div>
                
            </div>
        </div>
    )
}